#include <iostream> // imports input/output stream library
using namespace std; // uses standard namespace of C++ 

int main(){// default main class 
  
  cout << "Hello World" << endl; // "cout" = console out (prints in console keyword) :: "<<" is stream insertion operator :: "endl" is to indicate to program to end line. 
  return 0;// tells system program is ended by returning zero.
} 
